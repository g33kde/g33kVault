// Bumped automatically by a Claude Code hook right before every `git push`
// (see .claude/settings.json) — not a semantic version, just a running
// push counter shown in the /admin footer so it's easy to confirm which
// build is actually live. Do not hand-edit; the hook does a plain regex
// bump of the last segment.
export const APP_VERSION = '107.003';
